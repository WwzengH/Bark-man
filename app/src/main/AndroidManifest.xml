<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.superman">

    <application
        android:name=".app.Myapp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".song.Coursepayment2Activity"/>
        <activity android:name=".song.Coursepayment1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".song.JobReviewActivity">



        </activity>
        <activity android:name=".song.LogisticsDetailActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".song.MusicActivity" />
        <activity android:name=".song.OrderDetailActivity"></activity>
        <activity android:name=".song.MyOrderActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>